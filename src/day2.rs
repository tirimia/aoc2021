use crate::read_lines;

pub fn day_2() -> String {
    let _input: Vec<usize> = read_lines("assets/day2.input")
        .iter()
        .map(|l| l.parse::<usize>().unwrap())
        .collect();
    // format!("Part 1: {}\nPart 2: {}", day1a(input.clone()), day1b(input))
    "TODO".to_string()
}
